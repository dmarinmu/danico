@model IEnumerable<DanicoProject.Models.vQuote>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            Uname
        </th>
        <th>
            QrequestDate
        </th>
        <th>
            Qdescription
        </th>
        <th>
            QtripStartDate
        </th>
        <th>
            QtripEndDate
        </th>
        <th>
            idTripType
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Uname)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.QrequestDate)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Qdescription)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.QtripStartDate)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.QtripEndDate)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.idTripType)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
        </td>
    </tr>
}

</table>
